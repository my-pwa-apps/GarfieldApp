# September 7 Repair Pass

This records application changes after the historical review in [REVIEW-2026-09-07.md](REVIEW-2026-09-07.md). The initial repair pass made no commit, deployment, or live account/resource change and produced application version **1.0.13**. The subsequent authorized proxy deployment and version **1.0.14** are recorded below.

## Dedicated Proxy Follow-Up

Deployed `garfieldapp-corsproxy` at https://garfieldapp-corsproxy.garfieldapp.workers.dev with version ID `1cdd14ac-2919-45b5-ac20-7a55987a6e9c`. Its configuration pins the Garfield account and excludes DirkJan from its target allowlist. Account inspection established that the older `corsproxy` is a shared service; its DirkJan response label alone was not evidence of misrouting. Its ETag and modification time were verified unchanged after this deployment.

The frontend, CSP, preconnect, test fixtures, and live health check now use the dedicated endpoint. Legacy cached image URLs remain displayable, and sharing unwraps old proxy URLs before fetching through the new endpoint. The synchronized app version is **1.0.14**. No Pages deployment or commit was made.

Migration validation: 102 unit tests passed; 77 desktop/mobile browser tests passed with 7 intentional skips; 4 cross-browser smoke tests passed. Syntax, lint, asset verification, and changed-code editor diagnostics passed. Live checks passed for usage responses, Garfield preflight, rejected DirkJan targets, and rejected unrelated browser origins.

**R06 remains open, with corrected scope:** the custom-user-agent GoComics health request returned HTTP 403, but subsequent real-browser checks below show English works through both proxies. A uClick image probe returned upstream TLS error 526. Favorites `/top` passed. The initial-pass results below are historical, not proof of a general English outage.

### September 7 Browser Verification

The user's observation that production works was reproduced with installed Playwright Chromium, no mocks, and service workers blocked. Production fetched September 7 and September 6 English HTML through the shared proxy with HTTP 200 and decoded the September 7 GoComics image at 900x258. No public-proxy fallback was needed.

Identical April 29 English requests returned HTTP 200 and the same decoded image through both shared and dedicated proxies. Spanish requests for that date returned HTTP 403 through both. Only the cross-proxy diagnostic context bypassed the older production CSP, which does not yet allow the dedicated hostname; the clean production navigation used normal CSP enforcement. The integrated browser tool timed out, so verification used the installed Playwright browser instead.

Node header comparisons returned April English 403 with the health-check user-agent and 200 with browser-style headers on both proxies. This supports user-agent sensitivity, but cache effects and upstream variability were not eliminated. The current health check is not representative of observed English availability. Its implementation remains unchanged pending approval; R06 now covers that mismatch and the Spanish failure rather than alleged wrong deployment. No Worker/Pages deployment or application-code change was made in this follow-up.

## Disposition

### Subsequent Spanish Workflow Correction

The user's report that the live Spanish switch works was reproduced using the actual Settings checkbox in a fresh Chromium context, with normal CSP, no mocks, and service workers blocked. September 7 Spanish returned HTTP 200 through the shared proxy and decoded a distinct 900x258 image without a public-proxy fallback or error message.

A subsequent same-browser comparison found September 7 Spanish HTTP 200 with decoded image through the shared proxy, but HTTP 403 through the dedicated proxy. April 29 Spanish still returned 403 on both. Thus neither a general Spanish outage nor Spanish parity between proxies is established. The earlier suggestion to retain the dedicated proxy must not be interpreted as approval to publish the frontend migration. Hold publication while investigating cache state, deployed behavior, and upstream variability; do not attribute the difference to a proven cause yet. Only review documentation changed in this follow-up; production remains untouched.

Thirteen canonical findings are repaired in repository code: R01, R03-R05, R07-R13, R16, and R17. [BACKLOG.md](BACKLOG.md) records the changes and focused regression evidence. R02, R06, R14, and R15 remain open; this is not unconditional release sign-off.

## Release Conditions

1. **R02: Live Drive contract.** Verify media-read ETags and conditional PATCH/DELETE behavior with a real authorized account. Run two-device conflict, deletion/reconnect, first-creation, and account-switch scenarios. The implementation refuses unguarded overwrites and retains pending local intent. No credentials were used in this pass.
2. **R06: Representative live checks.** Deployment identity has been reconciled. Correct the health check to exercise the successful English browser workflow and investigate the observed Spanish failure. Preserve security restrictions and report degraded provider behavior explicitly.
3. **R14: Architecture.** The app is still 4,833 lines, above the 3,000-line target. New modules are below 800 lines, and the app is capped at 4,850 lines. Establish visual baselines before extracting the coupled toolbar, navigation, gesture, and leaderboard state further.
4. **R15: Performance.** The original three-run LCP <3.0 s, Speed Index <5.8 s, and performance >=0.80 criteria remain unmet. Keep live-provider audits separate from deterministic fixtures. Lighthouse was upgraded from 12 to 13, so scores are not directly comparable to the historical review.

## Validation

Final checks on the repaired version:

| Check | Result |
|---|---|
| Syntax, lint, asset guard | Passed |
| Unit suite | 100 passed, including the real SQLite transaction regression |
| Desktop/mobile browser suite | 77 passed, 7 intentional skips |
| Chromium, Firefox, WebKit, mobile Safari smoke | 4 passed |
| Complete npm audit | Zero vulnerabilities |
| Lighthouse 13 | Performance 0.71, accessibility 1.00, best practices 0.96; passes existing repository thresholds, not R15's target |
| Lighthouse timing | LCP 4.66 s, Speed Index 8.32 s, CLS 0.047 |
| Live favorites API | Passed |
| Live proxy | Failed: HTTP 403, `x-proxy-by: dirkjanapp-corsproxy` |
| Local preview | HTTP 200 at http://127.0.0.1:8000/ |

The release gates were completed as individual commands after long combined runs were interrupted by terminal-session reuse. **The overall predeploy gate remains blocked by live proxy health.** No local failure is being hidden by calling that gate green.

Focused tests cover initial/replacement image failure, touch keyboard focus, a real worker-version transition followed by decoded offline navigation, streamed UTF-8 limits, multipage cleanup, account-batched migration, merge/conflict/retry behavior, and OAuth timeout/retry with session-only token storage. Cross-browser validation also exposed a corrupt base64 test image that earlier tests suppressed; it now uses verified repository PNG bytes and checks decoded width.

The SQLite transaction regression runs the production leaderboard methods in Workerd with injected identity and post-write failure. It verifies real rollback and duplicate retry behavior, not Google's authentication service. The installed Miniflare version is pinned; its supported compatibility date is 2026-08-18, while deployment remains configured for 2026-08-27.

No native Windows host or real Google sign-in was exercised. Editor HTML hints about browser support for theme-color and fetchpriority remain; they predate this pass and do not indicate new JavaScript errors.